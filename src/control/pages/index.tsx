import {Box} from "@mui/material";
import React from "react";

interface Props {
  randKey?: number;
}

export const ControlHomePage: React.FC<Props> = (props: Props) => {
  return <Box>Home Page</Box>;
};
